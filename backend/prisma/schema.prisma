"datasource db {\n  provider = \"sqlite\"\n  url      = \"file:./dev.db\"\n}\n\ngenerator client {\n  provider = \"prisma-client-js\"\n}\n\nmodel User {\n  id           String        @id @default(uuid())\n  name         String\n  email        String        @unique\n  password     String\n  role         String        @default(\"volunteer\") // \"volunteer\" | \"admin\"\n  phone        String?\n  profileImage String?\n  createdAt    DateTime      @default(now())\n  updatedAt    DateTime      @updatedAt\n  volunteer    Volunteer?\n  activityLogs ActivityLog[]\n}\n\nmodel Volunteer {\n  id             String        @id @default(uuid())\n  userId         String        @unique\n  user           User          @relation(fields: [userId], references: [id], onDelete: Cascade)\n  age            Int?\n  gender         String?\n  address        String?\n  occupation     String?\n  skills         String        // Comma-separated list (e.g. \"React, Node, Communication\")\n  interests      String        // Comma-separated list (e.g. \"Teaching, Health, Environment\")\n  availability   String        // Days and times description\n  location       String?\n  hoursCompleted Float         @default(0.0)\n  resumeUrl      String?\n  status         String        @default(\"pending\") // \"pending\" | \"approved\" | \"rejected\"\n  events         Event[]       @relation(\"VolunteerEvents\")\n  tasks          Task[]\n  certificates   Certificate[]\n}\n\nmodel Event {\n  id          String      @id @default(uuid())\n  title       String\n  description String\n  date        DateTime\n  time        String      // e.g. \"10:00 AM - 02:00 PM\"\n  location    String\n  capacity    Int\n  imageUrl    String?\n  volunteers  Volunteer[] @relation(\"VolunteerEvents\")\n  createdAt   DateTime    @default(now())\n  updatedAt   DateTime    @updatedAt\n}\n\nmodel Task {\n  id                  String     @id @default(uuid())\n  title               String\n  description         String?\n  deadline            DateTime\n  status              String     @
<truncated 1236 bytes>